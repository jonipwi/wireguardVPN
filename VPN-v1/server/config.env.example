# WireGuard server configuration - EXAMPLE
# Copy this to config.env and customize for your setup

# Public endpoint of the server (domain or public IP) - REQUIRED
SERVER_HOST=vpn.example.com

# WireGuard UDP port
WG_PORT=51820

# Server interface address (CIDR) - the server gets this IP
WG_ADDRESS=10.8.0.1/24

# Allowed IPs for peers (route all traffic via VPN by default)
# For split tunneling, use something like: 10.8.0.0/24,192.168.1.0/24
CLIENT_ALLOWED_IPS=0.0.0.0/0, ::/0

# DNS servers pushed to clients
DNS=1.1.1.1, 1.0.0.1

# Linux interface to egress the Internet (auto-detected if empty)
# Common values: eth0, ens3, enp0s3
EXTERNAL_INTERFACE=

# Optional PostUp/PostDown additional rules (semicolon-separated bash commands)
# Example: EXTRA_POST_UP=iptables -A INPUT -p tcp --dport 22 -j ACCEPT
EXTRA_POST_UP=
EXTRA_POST_DOWN=

# Optional MTU for WireGuard interface (empty = auto)
# Use if you have MTU issues: 1420, 1380, etc.
WG_MTU=

# Keepalive for clients (seconds); set to 25 when behind NAT
PERSISTENT_KEEPALIVE=25

# Peer IP allocation start (server is .1). Example: 10.8.0.2
PEER_START_IP=10.8.0.2